# Cloudflare Pages — output dir only (wrangler cannot set build command).
# You MUST set in dashboard: Build command = npm run build
name = "worldcup2026-website"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
